// Numbers

10; // Integer

2.3; // Floating point

2.3 * 100; // => 229.99999999999999997 (REF IEEE754)

0.1 + 0.2; // => 0.3000000000000004 (REF IEEE754)

// integer
let a = 20;

// floating point
let b = 1.234;

// decide precision
(100.1234).toFixed(1);
// => 100.1

// strings. No difference between single and double qoutes.
let c = 'hei';

// String methods like 'slice'

// split string
'hello world'.split(' ');
// => ['hello', 'world']

// Booleans

// for loop
for (let i = 0; i < 10; i++) {
  console.log('Yeehaw');
}
